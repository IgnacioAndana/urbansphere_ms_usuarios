PORT=3001

DB_HOST=localhost
DB_PORT=3306
DB_NAME=porsusde_urbansphere
DB_USER=tu_usuario
DB_PASSWORD=tu_password

JWT_SECRET=urbansphere-jwt-secret-change-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

RABBITMQ_URL=amqp://guest:guest@localhost:5672

# Correo — Brevo/Gmail (recomendado: envía a cualquier destinatario)
EMAIL_PROVIDER=smtp
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=b0814e001@smtp-brevo.com
SMTP_PASS=7UOQ10LcFymw4trg
MAIL_FROM=nashogringo92@gmail.com

# Resend (solo pruebas: onboarding@resend.dev → solo tu email de cuenta)
# EMAIL_PROVIDER=resend
# RESEND_API_KEY=re_tu_api_key
# RESEND_FROM=onboarding@resend.dev

# Recuperación de contraseña
FRONTEND_URL=http://localhost:5173
PASSWORD_RESET_PATH=/restablecer-contrasena
PASSWORD_RESET_EXPIRES_IN=1h

# Logs HTTP en consola (pm2 logs). false para desactivar
HTTP_LOGGING=true

NODE_ENV=development

# TypeORM: usar false y ejecutar database/init-all.sql manualmente
DB_SYNCHRONIZE=false
DB_LOGGING=false
